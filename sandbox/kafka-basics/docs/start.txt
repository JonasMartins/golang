1) Run docker: docker compose up -d

2) Create a topic
docker compose exec broker-kafka-basics \
  kafka-topics --create \
    --topic purchases \
    --bootstrap-server localhost:9092 \
    --replication-factor 1 \
    --partitions 1

3) create util.go and producer.go

4) make consumer

5) make producer